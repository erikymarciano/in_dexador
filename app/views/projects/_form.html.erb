<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_area :price %>
  </div>
  
  <div class="field">
    <%= f.label :status %>
    <%= f.text_area :status %>
  </div>

  <div class="field">
    <%= f.label :drivelink %>
    <%= f.text_area :drivelink %>
  </div>
  
  <div class="field">
    <%= f.label :githublink %>
    <%= f.text_area :githublink %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
